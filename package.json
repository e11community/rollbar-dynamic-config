{
  "name": "@e11community/rollbar-dynamic-config",
  "version": "3.0.1",
  "description": "GitHub Action to generate the dynamic portions of Rollbar configuration",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "esbuild src/main.ts --bundle --platform=node --target=node22 --format=cjs --conditions=import --outfile=dist/index.js",
    "all": "npm run build && npm run package",
    "rebuild": "([ -d ./dist ] && rm -rf ./dist); ([ -d ./node_modules ] && rm -rf ./node_modules); NPM_CONFIG_UPDATE_NOTIFIER=false npm install --no-fund && npm run all"
  },
  "keywords": [
    "github-actions",
    "rollbar",
    "typescript"
  ],
  "author": "Engineering 11",
  "repository": {
    "url": "git+https://github.com/e11community/rollbar-dynamic-config.git",
    "type": "git"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.1.0",
    "@google-cloud/secret-manager": "^6.1.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "esbuild": "^0.28.0",
    "prettier": "^3.8.3",
    "typescript": "^5.9.3"
  }
}
